/*
import java.util.Arrays;

class ResistorColor {
    enum RING_COLORS {
        black, brown, red, orange, yellow, green, blue, violet, grey, white
    }

    int colorCode(String color) {
        return RING_COLORS.valueOf(color).ordinal();
    }

    String[] colors() {
        return Arrays.stream(RING_COLORS.values())
                .map(Enum::name)
                .toArray(String[]::new);
    }
}
*/
