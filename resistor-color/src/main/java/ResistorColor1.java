/*
import java.util.stream.IntStream;

class ResistorColor {
    String[] RING_COLORS = {"black", "brown", "red", "orange", "yellow", "green", "blue", "violet", "grey", "white"};

    int colorCode(String color) {
        return IntStream.range(0, RING_COLORS.length)
                .filter(i -> RING_COLORS[i].equals(color))
                .findFirst().orElse(-1);
    }

    String[] colors() {
        return RING_COLORS;
    }
}
*/
